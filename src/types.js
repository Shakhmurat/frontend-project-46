const types = {
  added: 'added',
  deleted: 'deleted',
  updated: 'updated',
  unchanged: 'unchanged',
  nested: 'nested',
};

export const {
  added,
  deleted,
  updated,
  unchanged,
  nested,
} = types;
